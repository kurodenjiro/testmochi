# Readable Mochi data

These Markdown snapshots mirror the final state of the Git bundles in `../artifacts`.
Each record is a Markdown table with stable field-level diffs. The bundles remain the complete history fixture.
